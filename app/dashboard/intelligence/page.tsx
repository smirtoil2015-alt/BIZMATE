import { demoInsights } from '@/lib/demo-data';

export default function IntelligencePage() {
  return <main className="module-page"><header><div><small>BIZMATE / INTELLIGENCE</small><h1>Intelligence</h1><p>Turn company signals into clear priorities, risks and opportunities.</p></div><button className="primary">✦ Analyze business</button></header><section className="ai-hero"><div><span>● BIZMATE INTELLIGENCE</span><h2>Your business, explained.</h2><p>What changed, why it matters, and what to do next — in one executive view.</p></div><div className="ai-score"><small>Business Health</small><strong>87</strong><span>/ 100</span></div></section><section className="insight-grid">{demoInsights.map(i => <article className={`insight-card ${i.severity}`} key={i.id}><div><span>{i.severity.toUpperCase()}</span><h3>{i.title}</h3><p>{i.description}</p></div><strong>{i.metric}</strong><button>Review →</button></article>)}</section><section className="data-card"><div className="data-head"><h2>Decision brief</h2><span className="status active">Approval-first AI</span></div><p className="brief">BIZMATE separates confirmed company facts from AI recommendations. Actions that affect money, communications or access require explicit approval.</p></section></main>;
}

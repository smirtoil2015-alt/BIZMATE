import '../module.css';
import { demoCustomers } from '@/lib/demo-data';

export default function CustomersPage() {
  return <main className="module-page"><header><div><small>BIZMATE / CUSTOMERS</small><h1>Customers</h1><p>Manage relationships, value and follow-up priorities from one place.</p></div><button className="primary">+ New customer</button></header><section className="module-grid"><div className="summary-card"><span>Active</span><strong>{demoCustomers.filter(c=>c.status==='active').length}</strong><small>customers</small></div><div className="summary-card"><span>Pipeline value</span><strong>${demoCustomers.reduce((s,c)=>s+(c.value??0),0).toLocaleString()}</strong><small>tracked value</small></div><div className="summary-card"><span>Follow-up</span><strong>{demoCustomers.filter(c=>c.status==='lead').length}</strong><small>need attention</small></div></section><section className="data-card"><div className="data-head"><h2>Customer portfolio</h2><input placeholder="Search customers..."/></div><div className="table"><div className="tr th"><span>Customer</span><span>Status</span><span>Value</span><span>Last contact</span></div>{demoCustomers.map(c=><div className="tr" key={c.id}><span><b>{c.name}</b><small>{c.company}</small></span><span><em className={`status ${c.status}`}>{c.status}</em></span><span>${(c.value??0).toLocaleString()}</span><span>{c.lastContactAt ?? '—'}</span></div>)}</div></section></main>;
}

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    function signedIn() {
      return request.auth != null;
    }

    function memberPath(orgId) {
      return /databases/$(database)/documents/organizations/$(orgId)/members/$(request.auth.uid);
    }

    function isMember(orgId) {
      return signedIn() && exists(memberPath(orgId));
    }

    function memberRole(orgId) {
      return get(memberPath(orgId)).data.role;
    }

    function isAdmin(orgId) {
      return isMember(orgId) && memberRole(orgId) in ['owner', 'admin'];
    }

    function canWriteBusinessData(orgId) {
      return isMember(orgId) && memberRole(orgId) in ['owner', 'admin', 'manager'];
    }

    function canWriteFinance(orgId) {
      return isMember(orgId) && memberRole(orgId) in ['owner', 'admin', 'manager'];
    }

    match /users/{userId} {
      allow read, create, update: if signedIn() && request.auth.uid == userId;
      allow delete: if false;
    }

    match /organizations/{orgId} {
      allow read: if isMember(orgId);
      allow create: if signedIn() && request.resource.data.ownerId == request.auth.uid;
      allow update: if isAdmin(orgId);
      allow delete: if false;

      match /members/{userId} {
        allow read: if isMember(orgId);
        allow create: if isAdmin(orgId) || (signedIn() && userId == request.auth.uid && request.resource.data.role == 'owner');
        allow update, delete: if isAdmin(orgId);
      }

      match /customers/{recordId} {
        allow read: if isMember(orgId);
        allow create, update, delete: if canWriteBusinessData(orgId);
      }

      match /projects/{recordId} {
        allow read: if isMember(orgId);
        allow create, update, delete: if canWriteBusinessData(orgId);
      }

      match /insights/{recordId} {
        allow read: if isMember(orgId);
        allow create, update, delete: if canWriteBusinessData(orgId);
      }

      match /finance/{recordId} {
        allow read: if isMember(orgId);
        allow create, update, delete: if canWriteFinance(orgId);
      }

      match /{document=**} {
        allow read: if isMember(orgId);
        allow create, update, delete: if canWriteBusinessData(orgId);
      }
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
